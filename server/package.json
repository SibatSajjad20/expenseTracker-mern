{
  "name": "expense-tracker-server",
  "version": "1.0.0",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "nodemon src/index.js",
    "build": "cd ../client && npm run build && cd ../server && xcopy ..\\client\\dist dist /E /I /Y",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["expense", "tracker", "mern", "api"],
  "author": "Your Name",
  "license": "MIT",
  "description": "Backend API for expense tracker application",
  "dependencies": {
    "bcrypt": "^6.0.0",
    "cookie-parser": "^1.4.7",
    "cors": "^2.8.5",
    "dotenv": "^17.2.2",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.18.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.10"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  }
}
